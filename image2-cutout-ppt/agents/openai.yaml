interface:
  display_name: "Image2 Cutout PPT"
  short_description: "Make polished AI figures into PPT assets"
  default_prompt: "Use $image2-cutout-ppt to first create a polished Image2 diagram, then derive a cutout-only editable PPT asset library."
policy:
  allow_implicit_invocation: true
